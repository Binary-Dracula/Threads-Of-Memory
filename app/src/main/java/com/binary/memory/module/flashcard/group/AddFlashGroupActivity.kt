package com.binary.memory.module.flashcard.group

import android.os.Bundle
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
import androidx.core.view.ViewCompat
import androidx.core.view.WindowInsetsCompat
import com.binary.memory.R
import com.binary.memory.base.DraculaActivity
import com.binary.memory.databinding.ActivityAddFlashGroupBinding

class AddFlashGroupActivity : DraculaActivity<ActivityAddFlashGroupBinding>() {
    override fun layoutId(): Int {
        return R.layout.activity_add_flash_group
    }

    override fun initView() {
        super.initView()

    }
}